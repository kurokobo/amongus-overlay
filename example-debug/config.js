
const auto_players = {
    fixed: false,
    // fixed: true,
    colors: [
        "Red",
        "Green",
        "Blue",
        "Black",
    ],
    update_delay_on_discuss: 5,
}

const auto_result = {
    timeout: 30,
}

const config = {

}